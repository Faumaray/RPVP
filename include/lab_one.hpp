#include <mpicxx.h>
#include <stdio.h>
#include <iostream>
#include <stdlib.h>
#include <vector>

void ring(size_t size);
void broadcast(size_t size);
void gather(size_t size);
void alltoall(size_t size);
